[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mx-closed-loop"
version = "0.1.0"
description = "Closed-loop EKF + Smith compensation scaffold for simglucose"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
